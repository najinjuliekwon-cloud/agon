name = "agon-proxy"
main = "worker.js"
compatibility_date = "2024-10-01"

[vars]
# 본인 GitHub Pages 주소로 바꾸세요. "*" 로 두면 누구나 호출할 수 있습니다.
ALLOWED_ORIGIN = "https://najinjuliekwon-cloud.github.io"

# GEMINI_API_KEY 는 여기에 적지 말고 아래 명령으로 등록하세요:
#   npx wrangler secret put GEMINI_API_KEY
